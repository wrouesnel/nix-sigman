output: api.gen.go
package: api
generate:
  echo5-server: true
  models: true
output-options:
  # to make sure that all types are generated
  skip-prune: true
import-mapping:
  ./types.yaml: github.com/wrouesnel/nix-sigman/pkg/serve/api/v1/types